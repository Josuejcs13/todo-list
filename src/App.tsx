import Button from "./components/button";

function App() {
  return (
    <div>
      <Button text="Adicionar" type="add"></Button>
      <Button text="Limpar" type="remove"></Button>
    </div>
  );
}

export default App;
